spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cprdb
    username: bear
    password: grylls
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  error:
    include-message: always

