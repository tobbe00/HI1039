spring:
  datasource:
    url: jdbc:mysql://localhost:3307/cprdb
    username: bear
    password: grylls
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
        show-sql: true